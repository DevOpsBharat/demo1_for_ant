<?xml version="1.0" encoding="UTF-8"?>
<project name="POC" default="test" basedir=".">
    <description>
        simple example build file
    </description>
	<property file="default.properties"/>
    
	<target name="deliver-strategies">
		<sshexec host="${app.ssh.host}" username="${app.ssh.user}" keyfile="deploy_environment.priv" password="${app.ssh.password}" port="${app.ssh.port}" trust="true" command="mkdir -p ${app.upload.path}/Strategies" verbose="${sshexec-verbose}" failonerror="false" />
	</target>
</project>